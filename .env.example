# Example .env for RetaliqHosts
# Allowed IPs (comma separated). Include 127.0.0.1 and your WSL host IP if needed.
RETALIQ_ALLOWED_IPS=127.0.0.1,172.17.0.1

# Optional API key required in header X-Api-Key
RETALIQ_API_KEY=replace-with-strong-secret
